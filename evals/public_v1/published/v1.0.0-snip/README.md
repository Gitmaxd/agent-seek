# Published run — v1.0.0 snip

Frozen live preference eval for suite `agent-seek-public-v1`.

| | |
| --- | --- |
| Run id | `20260921T144924Z` |
| Judge | `gpt-5.6-sol` · Responses API · `reasoning.effort=medium` · `mode=standard` |
| Mode | `snip` · k=10 · max_candidates=50 |
| Score | **37** Agent Seek · **0** tie · **13** baseline (You.com order) |
| Errors | 0 / 50 |

Same discovery pool per query for both sides; A/B blinding with mapping stored in run output only.

Merged from overnight judged rows (35) plus 2026-09-21 ~07:48 PT finish of the 15 TypeSafe-failed queries (`run_20260921T144855Z`). All 50 rows have `error: null`.
